# Задание 1
text = """Moscow is the capital and the largest city of Russia. The Kremlin is the heart of Moscow. 
Moscow provides opportunities for cultural time spending. There are a lot of theatres (the Bolshoi Theatre, 
the Maly Theatre, the Moscow Art Theatre, the Theatre in Taganka and many others), museums and art galleries 
(the Pushkin Museum of Fine Arts, the Tretyakov Gallery) to satisfy every taste."""

text1 = text.lower() # ШАГ 1 меняем на единый регистр, убирая заглавные буквы
# ШАГ 2 заменяем знаки препинания на пробелы. Апостроф просто убираем, так как он находится внутри слова, а не между
text2 = text1.replace('.',' ').replace(',',' ').replace('"',' ').replace("'",'').replace('!', ' ').replace('?', ' ')  
text_full = text2.split()  # ШАГ 3 делим на отдельные слова

counter = { } # создадим пустой словарь для дальнейшего подсчёта частот
for word in text_full:
    counter[word] = counter.get(word, 0) + 1 # get возвращает значение (частоту) ключа, если такого слова еще не было - вернет 0

for words in counter.keys():  # ключи словаря
    if counter[words] >= 3: # встречаются в тексте чаще, чем 3 раза
        print("Слово", words.title(), "встречается", counter[words], "раз.") 
        # Используем title, чтобы напечатать каждое слово (ключ) с заглавной буквы
        
#дополнение: print(counter) можно увидеть весь словарь; уникальные слова и как часто они встречаются в тексте

